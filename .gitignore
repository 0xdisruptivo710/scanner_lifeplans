node_modules
dist
.vercel
.env.local
.env.*.local
*.log
.DS_Store

# Build cache
*.tsbuildinfo

# Local tooling
.aiox-core/
.claude/

# Per-client secrets (tokens, keys) — never commit
client_vars_*.md
